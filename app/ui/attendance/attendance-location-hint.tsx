export function AttendanceLocationHint() {
  return (
    <p className="text-center text-xs text-gray-500">
      Each In/Out records GPS (browser will ask for permission). Fixes usually
      arrive within a few seconds; use HTTPS or localhost. For better accuracy,
      stand near a window or outside.
    </p>
  )
}
