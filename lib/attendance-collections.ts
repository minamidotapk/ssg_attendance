/** One document per IN→OUT pair: inserted on IN, updated on OUT. */
export const ATTENDANCE_SESSIONS_COLLECTION = "attendance_sessions"

/** Legacy: separate documents per IN / OUT (read-only fallback for photos). */
export const ATTENDANCE_LOGS_LEGACY_COLLECTION = "attendance_logs"
